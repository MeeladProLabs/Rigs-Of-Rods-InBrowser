# Rigs-Of-Rods-InBrowser

[Rigs of Rods](https://www.rigsofrods.org) compiled to WebAssembly and running
in the browser. No install, no download — open a page, click the canvas, play.

Playable build: **https://meeladprolabs.github.io/Rigs-Of-Rods-InBrowser/** (GitHub
Pages) or **http://localhost:8000/** locally — see [Run it locally](#run-it-locally).

## Play on GitHub Pages

The repo is set up so the compiled game runs directly from **GitHub Pages**:

1. Repo **Settings → Pages → Source: Deploy from a branch → branch `main`, folder
   `/ (root)`** → Save.
2. Wait a minute for the first deploy, then open the URL GitHub shows you
   (`https://<user>.github.io/Rigs-Of-Rods-InBrowser/`).

`.nojekyll` (at the repo root) disables Jekyll and the root `index.html`
instant-redirects to `web/`, where the game lives.

Three GitHub limitations are worked around automatically, no configuration needed:

- **Files over 100 MB are not allowed** — the game data file (`RoR.data`,
  ~112 MB) is split into `RoR.data.0`, `RoR.data.1`, `RoR.data.2` (each ~60 MB)
  and `web/RoR.js` is patched to download the chunks and concatenate them before
  mounting the game's file system.
- **SharedArrayBuffer needs COOP/COEP headers, which GitHub Pages cannot send** —
  `web/coi-sw.js` is a small service worker that injects
  `Cross-Origin-Opener-Policy: same-origin` and
  `Cross-Origin-Embedder-Policy: credentialless` at runtime. The first visit
  reloads once (automatic) so isolation is active before the game boots.
- **Fixed-function materials have no shaders on WebGL** — RTSS
  (Real-Time Shader System) auto-generates GLSL ES shaders at startup, and
  `scripts/fix_rtshader_es.py` ships a WebGL-compatible copy of the RTSS
  shader library (`rtshader.zip`) that the generated programs include.

> The `web/` folder is generated by `scripts/deploy-web.py` from the wasm build
> (see [Building from source](#building-from-source)). It contains the game
> itself (`RoR.js`, `RoR.wasm`, `RoR.data.N` chunks) plus the web shell
> (`index.html`, `repo.html`, `controls.html`, `js/`).

## Status

The goal is a fully working Rigs of Rods in the browser. What works today:

- **Main menu** — wallpaper, ImGui interface, terrain selection.
- **Terrain loading** — the game's terrain shader generation was ported to
  GLSL/GLSL ES. WebGL has no fixed-function pipeline, so every material needs
  explicit shaders; the **RTSS (Real-Time Shader System)** is initialized on
  the web build and auto-generates GLSL ES shaders for every fixed-function
  material (BaseWhite, particles, Hydrax water, plain object materials, mods,
  ...), and custom shaders were added for the menu wallpaper, the ImGui menu,
  the skybox cube map and the terrain renderer. Selecting a map and starting a
  game loads the terrain and renders it instead of a white screen.
- **Driving** — spawning a vehicle (CTRL+G or via a mod) works: the dashboard,
  wheels and body render instead of freezing. The freeze was the RTSS shader
  library's `switch` statement (invalid in GLSL ES 1.00); `fix_rtshader_es.py`
  now ships ES-valid shader libs (`FFPLib_AlphaTest` rewritten to if/else).
- **On-screen error reporting** — if the game hits a problem, the reason is
  shown directly in the page (top banner with a *Copy error* button), so errors
  are readable without opening the console or pasting giant logs.
- **Resizable screen** — drag the **corner** to resize width *and* height
  independently, the **right edge** for width only, or the **bottom edge** for
  height only. Double-click the game to toggle fullscreen, or press **⤡ Fit
  window**.
- **Mod repository** — [repo.html](web/repo.html) browses the official Rigs of
  Rods repository (975+ resources): search, category filter, thumbnails,
  ratings, downloads, and per-mod detail pages with download links.
- **CI** — CircleCI validates the web frontend on every push (see
  [.circleci/config.yml](.circleci/config.yml)).

Known limitations of the WebAssembly build:

- **Scripting (AngelScript) is non-functional.** AngelScript is compiled in
  "generic calling mode" (`AS_MAX_PORTABILITY`) on Emscripten, which rejects
  the native calling-convention registrations the game's script bindings use
  (`asCALL_THISCALL`/`asCALL_CDECL`). Every registration fails, so the whole
  script API is unavailable and terrain scripts (`default.as`) fail to load
  with "Invalid configuration". The game detects this and continues without
  scripts — the map, physics and vehicles all work. Making scripts run would
  require re-registering all bindings with `asCALL_GENERIC` wrappers (a large
  refactor) or implementing a WebAssembly native-call shim.
- Shadows, vegetation, water and other GPU-heavy features are best kept at low
  settings until the GLSL ES shader coverage is complete.
- The terrain's generated textures (blend/normal/composite maps) are created
  after the material is first prepared (an OgreTerrain ordering quirk that also
  occurs on desktop), so the terrain renders with flat splatting; Ogre logs a
  handful of "texture layer will be blank" lines at terrain load. Cosmetic,
  not a crash.
- The threaded build needs a cross-origin-isolated page (see the service-worker
  note above). A single-threaded rebuild is a possible future optimization.

## Run it locally

```bash
cd web
python -m http.server 8000
# then open http://localhost:8000/
```

Serve over HTTP — the game fetches `RoR.wasm` / `RoR.data.N` chunks and needs a
normal web server, not `file://`.

## Controls

See [controls.html](web/controls.html) in the browser (top-right link), or the
usual Rigs of Rods bindings (WASD + mouse).

## Repository & multiplayer

- **Mod repository** — implemented as a web page (`web/repo.html`) backed by
  the official Rigs of Rods API. Browsing/downloading works today; *in-game*
  install (dropping a downloaded mod zip into the game's content folder at
  runtime) is on the roadmap.
- **Multiplayer** — planned, not started yet.

## Building from source

See [BUILD.md](BUILD.md) — toolchain, exact CMake flags, and the list of
source patches applied to the upstream tree.

After building, regenerate `web/` with:

```bash
python scripts/deploy-web.py
```

This copies the build output, splits `RoR.data` into GitHub-friendly chunks and
patches `RoR.js` to download them.

## CI (CircleCI)

Every push runs `web-check`: a Node syntax-check of the inline scripts in the
HTML pages plus an asset-reference check. See `.circleci/config.yml`. To set
up CircleCI for this repo, follow the steps in
[BUILD.md → CircleCI](BUILD.md#circleci).

## Acknowledgements

- [Rigs of Rods](https://github.com/RigsOfRods/rigs-of-rods) and the OGRE
  engine — the actual game, compiled to wasm.
- This project only *hosts* the compiled game and its web shell; all game code
  belongs to its upstream authors.
