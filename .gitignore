# The compiled game binaries ARE committed so GitHub Pages can serve the game:
#   - web/RoR.js          (~230 KB)
#   - web/RoR.wasm        (~9 MB)
#   - web/RoR.data.N      chunks, each < 100 MB (GitHub's per-file limit)
# They are generated by scripts/deploy-web.py from the wasm build (see BUILD.md).

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
node_modules/
