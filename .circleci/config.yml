version: 2.1

jobs:
  web-check:
    docker:
      - image: cimg/node:18
    steps:
      - checkout
      - run:
          name: Install dependencies (none required — stdlib only)
          command: node --version
      - run:
          name: Syntax-check inline scripts and asset references
          command: node scripts/check-web.mjs

workflows:
  version: 2
  build:
    jobs:
      - web-check
